name: goya24_flutter
description: The goya24 AI support messenger for Flutter — full screen, Persian and English, with the signed-in user introduced with proof.
version: 0.1.0
homepage: https://goya24.com
repository: https://github.com/Goya-24/goya24-flutter
issue_tracker: https://github.com/Goya-24/goya24-flutter/issues
topics:
  - chat
  - customer-support
  - live-chat
  - messenger
  - webview

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  webview_flutter_platform_interface: ^2.10.0

flutter:
